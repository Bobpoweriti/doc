<link rel="stylesheet" href="{{{uiRootPath}}}/css/site.css">
<style>
/* ===== TOPBAR NAVIGATION ===== */
.navbar .home-link {
  filter: brightness(0) invert(1);
}
.edit-this-page {
  display: none !important;
}
.navbar-dropdown {
  background: #2d2d2d;
  border-top: none;
  box-shadow: 0 4px 12px rgba(0,0,0,.5);
}
.navbar-dropdown .navbar-item {
  color: rgba(255,255,255,.87);
}
.navbar-dropdown .navbar-item:hover {
  background: rgba(255,255,255,.1);
  color: #fff;
}
.navbar-brand .home-link {
  color: #fff;
  font-weight: 600;
}
.navbar-search {
  flex: 1;
  max-width: 380px;
  padding: 0 .75rem;
}
#search-input {
  width: 100%;
  background: rgba(255,255,255,.12);
  border: 1px solid rgba(255,255,255,.25);
  border-radius: 4px;
  color: #fff;
  padding: .35rem .75rem;
  font-size: .9rem;
  outline: none;
}
#search-input::placeholder {
  color: rgba(255,255,255,.55);
}
#search-input:focus {
  background: rgba(255,255,255,.2);
  border-color: rgba(255,255,255,.5);
}
@media (max-width: 768px) {
  .navbar-search { display: none; }
  .navbar-brand { width: 100%; justify-content: space-between; }
  .navbar-end { flex-direction: column; width: 100%; }
  .navbar-end .navbar-item { justify-content: flex-start; }
}

/* ===== PAGE D'ACCUEIL CARTES ===== */
article:has(.home-hero) h1.page {
  margin-bottom: .25rem;
}
.home-hero {
  padding: 0 0 2rem;
}
.home-subtitle {
  color: #666;
  font-size: .95rem;
  margin: 0 0 3rem;
}
.home-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1rem;
}
@media (max-width: 768px) {
  .home-grid { grid-template-columns: repeat(2, 1fr); }
}
@media (min-width: 769px) and (max-width: 1024px) {
  .home-grid { grid-template-columns: repeat(3, 1fr); }
}
.home-card {
  border-radius: 10px;
  border: 2px solid #ddd;
  padding: 1.5rem 1rem;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  text-decoration: none;
  color: inherit;
  background: #fff;
  transition: transform .15s, box-shadow .15s;
  gap: .75rem;
}
a.home-card:hover {
  transform: translateY(-3px);
  box-shadow: 0 4px 16px rgba(0,0,0,.1);
  text-decoration: none;
}
.home-card--soon { opacity: .55; cursor: default; }
.home-card--blue   { border-color: #3b82f6; }
.home-card--orange { border-color: #f97316; }
.home-card--green  { border-color: #22c55e; }
.home-card--purple { border-color: #a855f7; }
.home-card--cyan   { border-color: #06b6d4; }
.home-card--yellow { border-color: #eab308; }
.home-card-title {
  font-size: 1rem;
  font-weight: 700;
  color: #1a1a1a;
}
.home-card-badge {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 2rem;
  height: 2rem;
  padding: 0 .6rem;
  border-radius: 9999px;
  font-size: .85rem;
  font-weight: 700;
  color: #fff;
}
.home-card--blue   .home-card-badge { background: #3b82f6; }
.home-card--orange .home-card-badge { background: #f97316; }
.home-card--green  .home-card-badge { background: #22c55e; }
.home-card--purple .home-card-badge { background: #a855f7; }
.home-card--cyan   .home-card-badge { background: #06b6d4; }
.home-card--yellow .home-card-badge { background: #eab308; }
.home-card-badge--soon {
  background: #d1d5db !important;
  color: #6b7280;
  font-size: .75rem;
  font-weight: 500;
}
.home-card-desc {
  font-size: .8rem;
  color: #888;
  margin-top: .2rem;
}
.home-card-icon-wrap {
  position: relative;
  display: inline-block;
  margin: 0 auto .75rem;
}
.home-card-icon {
  font-size: 1.75rem;
  width: 3.25rem;
  height: 3.25rem;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 10px;
}
.home-card--blue   .home-card-icon { background: rgba(59,130,246,.12); }
.home-card--orange .home-card-icon { background: rgba(249,115,22,.12); }
.home-card--purple .home-card-icon { background: rgba(168,85,247,.12); }
.home-card--green  .home-card-icon { background: rgba(34,197,94,.12); }
.home-card--cyan   .home-card-icon { background: rgba(6,182,212,.12); }
.home-card--yellow .home-card-icon { background: rgba(234,179,8,.12); }
.home-card--rose   .home-card-icon { background: rgba(244,63,94,.12); }
.home-card-icon-wrap .home-card-badge {
  position: absolute;
  top: -7px;
  right: -7px;
  min-width: 1.3rem;
  height: 1.3rem;
  padding: 0 .3rem;
  font-size: .72rem;
}
.home-card--red    { border-color: #ef4444; }
.home-card--rose   { border-color: #f43f5e; }
.home-card--teal   { border-color: #14b8a6; }
.home-card--teal   .home-card-badge { background: #14b8a6; }
.home-card--teal   .home-card-icon  { background: rgba(20,184,166,.12); }
.home-card--indigo { border-color: #6366f1; }

/* ===== NAV TOUJOURS DÉROULÉ ===== */
.nav-item > .nav-list {
  display: block !important;
}
.nav-item > .nav-item-toggle {
  transform: rotate(90deg);
}

/* ===== SÉPARATION CONTENU / NAV ===== */
main.article {
  background-color: #ffffff;
}
.nav-panel {
  background-color: #f8f9fb;
  border-right: 1px solid #dde1e7;
}
.toc-panel {
  background-color: #f8f9fb;
  border-left: 1px solid #dde1e7;
}

/* ===== IMPRESSION ===== */
@media print {
  #topbar,
  .nav-panel,
  .toc-panel,
  .breadcrumbs,
  .toolbar,
  .edit-this-page,
  footer.footer {
    display: none !important;
  }

  .body {
    display: block !important;
  }

  main.article {
    max-width: 100% !important;
    margin: 0 !important;
    padding: 1cm 0 0 0 !important;
  }

  h1, h2, h3, h4 { page-break-after: avoid; }
  table, .admonitionblock { page-break-inside: avoid; }

  @page { margin: 2cm; }
}
</style>
